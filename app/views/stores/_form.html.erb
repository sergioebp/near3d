<%= form_with(model: store, local: true) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.label :address %>
    <%= f.text_field :address %>

    <%= f.submit %>
<% end %>